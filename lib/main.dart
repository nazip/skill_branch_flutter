import 'package:flutter/material.dart' show runApp;

import 'app.dart';

// import 'user_holder.dart';

void main() {
  runApp(MyApp());
  // UserHolder users = UserHolder();
  // users.registerUserByEmail("a b", 'a@a.com');
  // users.registerUserByEmail("a b", 'a@a.com');
  // users.registerUserByEmail("a b", 'a@a.com');
}
